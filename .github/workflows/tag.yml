# This is a basic workflow to help you get started with Actions

name: Discord

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  tag-new-versions:
    runs-on: ubuntu-latest
    steps:

      - name: Send Discord Release Notification
        uses: meisnate12/discord-release-action@d45d1d320773f2db5dde5d1e7c4024c19bbc4fbd
        with:
          webhook_id: ${{ secrets.RELEASE_WEBHOOK_ID }}
          webhook_token: ${{ secrets.RELEASE_WEBHOOK_TOKEN }}
          description: TESTING
          title: WHYYYYY


      - name: Send Discord Release Notification
        uses: meisnate12/discord-release-action@master
        with:
          webhook_id: ${{ secrets.RELEASE_WEBHOOK_ID }}
          webhook_token: ${{ secrets.RELEASE_WEBHOOK_TOKEN }}
          description: TESTING
          title: WHYYYYY

